#!/usr/bin/env zsh

cd ~/Projects/labidw-etl
echo "Starting daily update"
for dir in $(cat .packages);
do
    echo "\nRunning $dir";
    pipenv run python -c "from $dir import lambda_function ; lambda_funtion(None, None)";
done
echo "\nFinished updating"
cd "$OLDPWD"
