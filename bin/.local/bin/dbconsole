#!/usr/bin/env bash

RUNNING=$(ps aux | awk '/jupyter-console --kernel=dbcrawler/ && !/awk/ {print $2}')
if [[ -n "$RUNNING" ]]; then
    kill $RUNNING
fi

tmuxinit -d ~/Projects/db-crawler -n console "jupyter console --kernel=dbcrawler"
